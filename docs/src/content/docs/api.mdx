---
title: API dla programistów
description: Przewodnik po API FunnyGuilds dla programistów.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

:::note
FunnyGuilds API Nightly 4.13.0
:::

## FunnyGuilds API

<Tabs syncKey="package-manager">
<TabItem label="Maven">
```xml
<repository>
    <id>reposilite-snapshots-repository</id>
    <url>https://maven.reposilite.com/snapshots</url>
</repository>
```
```xml
<dependency>
    <groupId>net.dzikoysk.funnyguilds</groupId>
    <artifactId>plugin</artifactId>
    <version>4.13.1-SNAPSHOT</version>
    <classifier>all</classifier>
    <scope>provided</scope>
</dependency>
```
</TabItem>
<TabItem label="Gradle Groovy">
```groovy
repositories {
    mavenCentral()
    maven {
        name 'reposilite-snapshots-repository'
        url 'https://maven.reposilite.com/snapshots'
    }
}
```
```groovy
dependencies {
    implementation 'net.dzikoysk.funnyguilds:plugin:4.13.1-SNAPSHOT:all'
}
```
</TabItem>
<TabItem label="Gradle Kotlin">
```kotlin
repositories {
    mavenCentral()
    maven {
        name = "reposilite-snapshots-repository"
        url = uri("https://maven.reposilite.com/snapshots")
    }
}
```
```kotlin
dependencies {
    implementation("net.dzikoysk.funnyguilds:plugin:4.13.1-SNAPSHOT:all")
}
```
</TabItem>
</Tabs>

#### FunnyGuilds

Główna instancja pluginu. Powinna zostać pobrana tylko raz np.:

```java
public class MyPlugin extends JavaPlugin {

    private FunnyGuilds funnyGuilds;
    
    @Override
    public void onEnable() {
        funnyGuilds = FunnyGuilds.getInstance();
    }
    
}
```

### User

Dostępne metody pobierania użytkownika:

- `UserManager#findByName(String)`
- `UserManager#findByPlayer(Player)`
- `UserManager#findByUuid(UUID)`

Przykład użycia:

```java
UserManager userManager = funnyGuilds.getUserManager();
Option<User> userOption = userManager.findByName("PlayerName");

if (userOption.isPresent()) {
    User user = userOption.get();
}
```

### Guild

Dostępne metody pobierania gildii:

- `GuildManager#findByName(String)`
- `GuildManager#findByTag(String)`
- `GuildManager#findByUuid(UUID)`
- `User#getGuild()`

Przykład użycia:

```java
GuildManager guildManager = funnyGuilds.getGuildManager();
Option<Guild> guildOption = guildManager.findByTag("TAG");

if (guildOption.isPresent()) {
    Guild guild = guildOption.get();
}
```

```java
Option<Guild> guildOption = user.getGuild();

if (guildOption.isPresent()) {
    Guild guild = guildOption.get();
}
```

### Nasłuchiwanie wydarzeń

Wydarzenia związane ze:
- Zmianami rankingu: [net/dzikoysk/funnyguilds/event/rank](https://github.com/FunnyGuilds/FunnyGuilds/tree/master/plugin/src/main/java/net/dzikoysk/funnyguilds/event/rank)
- Gildiami: [net/dzikoysk/funnyguilds/event/guild](https://github.com/FunnyGuilds/FunnyGuilds/tree/master/plugin/src/main/java/net/dzikoysk/funnyguilds/event/guild)
- Członkami gildii: [net/dzikoysk/funnyguilds/event/guild/member](https://github.com/FunnyGuilds/FunnyGuilds/tree/master/plugin/src/main/java/net/dzikoysk/funnyguilds/event/guild/member)
- Sojuszami: [net/dzikoysk/funnyguilds/event/guild/ally](https://github.com/FunnyGuilds/FunnyGuilds/tree/master/plugin/src/main/java/net/dzikoysk/funnyguilds/event/guild/ally)

Przykład z `GuildDeleteEvent`

```java
public class GuildDeleteListener implements Listener {

    @EventHandler
    public void onGuildDelete(GuildDeleteEvent event) {
        Guild guild = event.getGuild();
        User doer = event.getDoer();
        FunnyEvent.EventCause eventCause = event.getEventCause();

    }
    
}
```

Rejestracja w metodzie `onEnable()`:

```java
@Override
public void onEnable() {
    this.getServer().getPluginManager().registerEvents(new GuildDeleteListener(), this);
}
```
